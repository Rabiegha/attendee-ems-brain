# Journal — Semaine 29 (13 → 19 juillet 2026)

> **Journal de travail hebdomadaire.** Reconstitué depuis les commits git (tous repos) + décisions du
> workspace. Un fichier par semaine ISO. Source de vérité pour standup / suivi perso.
> **Réfs :** [NOW.md](../NOW.md) · [00-plan-action LFD 2026](../../workstreams/en-cours/lfd2026/00-plan-action.md) · [semaine précédente](./2026-W28.md)

---

## 🗓️ Lundi 13 juillet — Naissance du chantier T (tests event-critical) + vérif du code de scan

Thème : transformer le verdict Codex (« les E2E actuels ne suffisent pas ») en **chantier piloté**,
et vérifier dans le code réel ce que le scan fait vraiment aujourd'hui. **Session doc + investigation code, 1 push brain.**

**🧪 Chantier T créé — tests event-critical** _(commit `bcf926a`, +308 lignes)_

- Nouveau dossier [T-tests-event/](../../workstreams/en-cours/lfd2026/T-tests-event/README.md) : liste **P0** (health, scan QR event, permissions) / **P1** (PDF-badges, auth KO, queue BullMQ) / **P2** (capacité Redis, Playwright scan) — **statut : liste à valider avant de coder**. Ligne **T** ajoutée au [suivi des chantiers](../../workstreams/en-cours/lfd2026/03-suivi-chantiers.md) (~1,25–2,5 j).
- Clarification conceptuelle : **pas de 4e couche « intégration »** — nos E2E API (supertest + vrai Postgres/Redis) _sont_ des tests d'intégration. Les vrais trous d'intégration (BullMQ, capacité Redis) ajoutés en P1/P2.
- Décisions de périmètre : **pas de paiement pour cet event** → tests paiement supprimés · **tests sessions/inscriptions → chantier H** (impossibles avant les règles métier H).

**🔍 Vérif du code scan réel (`registrations.service.ts`) — 3 constats**

- ✅ **HMAC strict et branché** sur la route (`checkInByCode` → `ScanCodeResolverService`, UUID brut refusé) — mais **0 E2E sur la route** (unitaire seulement) → c'est exactement la valeur de T2.
- ⚠️ **Déjà scanné = 409 `ALREADY_CHECKED_IN`** + état serveur complet — pas un 2xx, mais choix délibéré pour la **réconciliation offline du mobile**. Reco : garder ce contrat, T2 le fige.
- 🔴 **Le re-scan n'est PAS loggé** : l'exception est levée, rien n'est persisté (`AuditLog` existe dans Prisma mais n'est jamais appelé). La règle « garder l'action » n'est pas implémentée → **dev à ajouter (H-D1)**.
- Clarifié au passage : cycle de vie d'un QR = **multi-scan légitime** entre points de contrôle (entrée → sessions → checkout → re-checkin) ; « déjà scanné » = re-scan au **même** point de contrôle. + rappel rôle HMAC/chantier D (signature `v1.<id>.<sig>`, anti-falsification).

**📦 Tests + devs sessions transférés au chantier H (owner Corentin)**

- Nouveau fichier [tests-sessions.md](../../workstreams/en-cours/lfd2026/H-inscriptions-session/tests-sessions.md) : règles métier actées (déjà scanné ≠ refus dur + loggé · pas sur event = refus · session **privée** avec inscription vs **publique** présence event suffit · droits scanner selon H), **14 tests** (H-T1→T14 : scan session, inscriptions, CRUD, ouverture/fermeture) + **7 devs pré-requis** (H-D1→D7) avec mapping dev ↔ tests débloqués.

**⚙️ Stratégie CI actée + doc post-event**

- Question tranchée : « beaucoup de tests, ça alourdit la CI ? » → **tout lancer à chaque PR** (pas de sélection par release), jobs parallèles, ~2-5 min à notre échelle. Optimisations (étagement par branche, tests affectés) consignées dans [ci-cd-post-event.md](../../workstreams/en-cours/lfd2026/0-fondations/ci-cd-post-event.md) avec déclencheurs (CI >10 min).
- [ci-cd-coordination.md](../codex-claude/ci-cd-coordination.md) mis à jour : encart 13/07 qui amende la demande à Claude (garder T1/T2/T3, retirer registration → H, retirer paiement).

---

## �️ Lundi 13 juillet (suite, après-midi + soirée) — Pipeline CI/CD validé de bout en bout + ops divers

Thème : exécution de la checklist [finalisation-ci-cd-et-livraison.md](../../workstreams/en-cours/lfd2026/0-fondations/finalisation-ci-cd-et-livraison.md) —
prérequis ops → audit nginx → fixes CI → **chaîne CI → GHCR → CD staging → VPS validée en conditions réelles**.

**🔑 Prérequis ops (phase 1) — tous faits**

- Clé SSH dédiée GHA (`gha_ems_deploy`, ed25519) générée + poussée sur le VPS · secrets `VPS_HOST`/`VPS_USER`/`VPS_SSH_KEY` sur les repos back+front via `gh secret set` · PAT GHCR `read:packages` loggé sur le VPS (root + debian).

**🔍 Audit nginx (phase 2) — 2 incidents découverts et corrigés**

- `/opt/ems-attendee/backend/frontend` = **dossier mort** (912 Mo), aucun conteneur ne le monte — nettoyage planifié (phase 4).
- 🔴 **Vhost staging disparu du VPS** (404 depuis ~3 j — commit orphelin balayé par un checkout) → restauré à chaud (`nginx -s reload`, zéro coupure) + **commité sur dev/staging/main** (plus jamais untracked).
- 🔴 **Bug swap prod front** : le deploy prod écrasait `dist/staging` → corrigé dans `cd-prod.yml` front + **CD staging front créé** (commit `bca6eca`).

**🧪 CI back : 3 fixes pour arriver au vert (18/18 e2e)**

1. `REDIS_HOST` absent du `.env.test` CI → BullMQ bouclait, **job pendu 3h49** → `localhost` + timeout 15 min.
2. **Seed e2e inexistant** : les specs supposaient des users jamais créés par aucun seeder du repo (hash faux dans `seed-dev.sql`, rôle `staff` absent) → `test/seed-e2e.ts` créé (réutilise les seeders dev) + step CI.
3. Throttler `@Throttle(10/min)` sur `/auth/login` → 429 en pleine suite (`skipIf NODE_ENV=test`) + assertions cookie obsolètes alignées sur le code réel.

**🚀 CD staging back validé + workflows sur `main`**

- Découverte GitHub : `workflow_run`/`workflow_dispatch` **exigent le workflow sur la branche par défaut** → merge `staging → main` (`4362745`, rien ne se déploie en auto).
- Dispatch CD staging → **success** : `ems-staging-api` tourne sur l'image GHCR du sha exact, `/api/health` → db/redis/migrations ok. **⏸ CD prod back/front : EN ATTENTE** (fenêtre 22h-06h + décision consciente : `main` = nouveau code désormais).
- Learnings consignés : [2026-07-13-branche-dediee-vs-vraies-branches-worktree.md](../../learnings/2026-07-13-branche-dediee-vs-vraies-branches-worktree.md).

**🤝 Point avec Corentin — nouvelle méthode de travail CI/CD** _(fait)_

- Explication de la nouvelle méthode de travail avec le CI/CD, revue de ce qui était fait jusque-là, et **répartition des tâches pour la semaine**.

**🔐 Certificat SSL itforbusinesslesclub.fr renouvelé** _(fait — hors attendee, LOF)_

**📧 Mailgun — Impossible d'activer un abonnement (Billing)** 🔴

- **Contexte** : accès OVH obtenus ✅ → lancement du warm-up. Mais lors de la souscription au plan Mailgun (32 €/mois), la page de paiement reste bloquée : le bouton **"Purchase" reste désactivé** alors que tous les champs obligatoires sont correctement remplis (CB, adresse de facturation…).
- **Investigation** : vérification des champs · navigation privée · sans extensions (AdBlock) · console · onglet Network.
- **Résultats** : 4 requêtes en **404** systématique — `GET /v1/mailforce/customer?full_details=true|false` et `GET /v1/mailforce/plan?full_details=true|false` → le front Mailgun ne récupère ni le customer ni le plan. Erreur console annexe : `RS SDK Error: A timeout of 11000 ms occurred while trying to check the ready status…` (SDK tiers A/B testing/analytics, probablement pas la cause).
- **Hypothèse** : problème **côté Mailgun** (provisionnement du compte ou bug du portail Billing), pas la CB ni le navigateur.
- **Impact** : impossible de souscrire au plan → mais **PAS bloquant** : le **warm-up démarre avec le compte starter** en attendant.
- **Actions suivantes** : vérifier le provisionnement du compte · contacter le support Mailgun si ça persiste · **réessayer le purchase MERCREDI 15/07 (très important)** → rappel dans l'[INBOX](../INBOX.md).

---

## 🌙 Lundi 13 juillet (soirée, fin) — Alignement complet des branches + blocage branch protection

Thème : finir l'alignement `staging`/`main` sur back **et** front, puis tenter le verrouillage. **Session assistée Claude.**

**🔀 Branches alignées partout — staging = main sur les deux repos**

- **Back** : cherry-pick du commit prettier `8b53222` (resté orphelin sur `chore/ci-cd`) → `staging` (`0aa540f`), puis merge `staging → main` (`b50339a`). État vérifié : `infra/staging-stack` (stack staging isolée + pgBouncer) déjà intégralement mergée dans staging/main.
- **Front** : merge `chore/ci-cd → staging` (`c274cf4`) — 2 conflits (`eventsApi.ts`, `EventSessionsTab.tsx` : features sessions vs prettier 14k) résolus en gardant la version sessions + prettier réappliqué · vérifs `tsc`/build/eslint vertes · puis fast-forward `staging → main`. **La CI/CD front (ci + cd-staging + cd-prod) est enfin sur `main`** — avant ça, `main` front n'avait AUCUN workflow.
- Branches désormais supprimables : `chore/ci-cd` (back+front), `infra/staging-stack`.

**🔴 Branch protection : BLOQUÉE par le plan GitHub Free**

- `gh` CLI installé (brew) + authentifié (compte Rabiegha, scopes repo/workflow).
- Tentatives branch protection API **et** rulesets → HTTP 403 « Upgrade to GitHub Pro » : **indisponible sur repos privés en plan Free** (l'hypothèse « OK sans org » du plan 0-CI était fausse).
- Options : **GitHub Pro ~4 $/mois** (recommandé) · org Team · repos publics (non). Checks requis déjà identifiés (back : `lint-typecheck`/`unit-tests`/`e2e-tests`/`build-image` · front : `lint-typecheck`/`unit-tests`/`build`/`e2e-smoke`) — à rejouer dès le plan upgradé.
- Le doc [finalisation-ci-cd-et-livraison.md](../../workstreams/en-cours/lfd2026/0-fondations/finalisation-ci-cd-et-livraison.md) a été **déplacé dans le dossier du chantier 0-fondations** (§3bis + §5 mis à jour).

**🚀 CD staging front rodé en conditions réelles — chaîne auto validée des 2 côtés**

- Premier vrai run CI front → 2 échecs découverts et corrigés (`498dd45`) : vitest ramassait les specs **Playwright** (`exclude` + `passWithNoTests` — constat : **0 test unitaire vitest**, à combler via T) · directive `eslint-disable` inutile.
- Push staging → CI verte → `workflow_run` → **CD staging front SUCCESS** (swap atomique, healthcheck) : staging.attendee.fr 200, prod 200 intacte. Le CD staging **back** auto s'était aussi déclenché (API staging sur `0aa540f`). **Le déclenchement auto est rodé back + front.**

**🏁 CD PROD back + front VALIDÉS — la chaîne CI/CD complète est rodée (fenêtre 22h-06h)**

- **CD prod back, 3 runs** : ① ÉCHEC + 🔴 **prod 502 ~7 min** — l'API démarrait bien mais 2 bugs latents : healthcheck compose `wget -4` (flag inexistant en BusyBox → conteneur unhealthy à vie, fix perdu de `staging-archive-2026-06-25`…) + **nginx ne re-résout pas l'upstream `api` après recreate** (IP figée → 502) ; et « premier deploy registry » = pas de `rollback_sha` → intervention manuelle (`nginx -s reload`, prod re-up). Fixes `4dd9092` : reload nginx intégré à `deploy_image()` + healthcheck sans `-4` + `deployed_sha` armé sur le VPS. ② deploy OK mais healthcheck public 404 (URL sans préfixe `/api`) → fix `aca1b49`. ③ **100 % VERT** (guard, scan migrations, verify-ci, deploy 1m20, notify).
- **CD prod front, 3 runs** : ① bloqué par `verify-ci` — `GITHUB_TOKEN` front sans `actions:read` (403) → bloc `permissions` ajouté (`5c2abe3`). ② échec healthcheck (curl `127.0.0.1` sans SNI → vhost api → 404) mais **ROLLBACK AUTO RÉUSSI en réel** 🎯 (prod et staging intacts) → fix `--resolve attendee.fr:443:127.0.0.1`. ③ **100 % VERT** (deploy 1m50, dist/staging préservé).
- Vérifs finales : prod front 200 · staging front 200 · API prod 200 (version = SHA exact).
- **Learning clé** : nginx fige l'IP de l'upstream au chargement → tout deploy qui recrée le conteneur api DOIT reload nginx. Et un healthcheck qui passe par nginx teste nginx, pas seulement l'API.

---

## 📊 Synthèse semaine (en cours)

| Chantier        | Δ cette semaine                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **T (nouveau)** | Créé, liste P0/P1/P2 posée — en attente de validation                                                                                                                                                                                                                                                                                                                                                                                      |
| **H**           | Enrichi : tests-sessions.md (H-T1→T14) + devs pré-requis (H-D1→D7, dont 🔴 log re-scan)                                                                                                                                                                                                                                                                                                                                                    |
| **0-CI**        | **CHAÎNE COMPLÈTE VALIDÉE EN RÉEL : CI + CD staging + CD prod × back + front.** Prérequis ops ✅ · CI verte 18/18 · branches alignées staging=main · incident 502 prod ~7 min au 1er CD prod back (2 bugs latents fixés) · rollback front **validé en réel** — reste : nettoyage legacy, branches mergées à supprimer, 🔴 branch protection (GitHub Free → Pro requis), 0 test unitaire front (→ T) |
| **C1**          | Accès OVH obtenus ✅ → warm-up Mailgun démarré en **starter** · 🔴 purchase du plan bloqué (bug billing Mailgun, 404 `/v1/mailforce/*`) → retry mercredi 15/07                                                                                                                                                                                                                                                                             |
| **Hors LFD**    | Certificat SSL itforbusinesslesclub.fr renouvelé (LOF)                                                                                                                                                                                                                                                                                                                                                                                     |
